= styles_for :account

= render 'entities/title_bar'

.remote#create_account{ hidden }

= render 'search'

.remote#options{ hidden }

.list#accounts
  - if @accounts.any?
    = render @accounts
  - else
    = render "shared/empty"

#paginate= render "shared/paginate_with_per_page"
#export= render "shared/export"

%iframe#uploading{ name: "uploading", style: "width:1px; height:1px; display: none;" }
