#confirm{ hidden }
.tlink
  = link_to("Back to List of Contacts", contacts_path) + " | "
  = link_to("Edit", edit_contact_path(@contact)) + " | "
  = link_to_function("Delete?", confirm_delete(@contact))
.title= @contact.full_name

