- edit ||= false
- collapsed = session[:contact_comment].nil? # && @contact.errors.empty?
= subtitle :contact_comment, collapsed, t(:comment)
.section
  %small#contact_comment_intro{ hidden_if(!collapsed) }
    = t(:comment_intro)
  #contact_comment{ hidden_if(collapsed) }
    = text_area_tag :comment