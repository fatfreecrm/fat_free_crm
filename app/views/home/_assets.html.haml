- sort_by_menu_items = Activity::ASSETS.inject([]) do |items, (key, value)|
  - items << %Q[{ name: "#{value}", on_select: function() { #{redraw(:asset, [ key, value.downcase ], url_for(:action => :redraw))} } }]

:plain
  new crm.Menu({
    trigger   : "asset",
    fade      : 0.5,
    appear    : 0.5,
    menu_items: [ #{sort_by_menu_items.join(",")} ]
  });
