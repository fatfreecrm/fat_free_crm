page[dom_id(@opportunity)].visual_effect "BlindUp", :duration => 0.25
if called_from_index_page?
  page << refresh_sidebar(:index, :filters)
else
  page[:recently].replace :partial => "common/recently"
end