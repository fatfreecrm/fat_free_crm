- edit ||= false
- collapsed = session[:opportunity_comment].nil? # && @opportunity.errors.empty?
= subtitle :opportunity_comment, collapsed, t(:comment)
.section
  %small#opportunity_comment_intro{ hidden_if(!collapsed) }
    = t(:comment_intro)
  #opportunity_comment{ hidden_if(collapsed) }
    = text_area_tag :comment