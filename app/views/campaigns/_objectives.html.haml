.subtitle= subtitle :objectives, "Campaign Objectives"
.section
  #objectives_intro{ hidden_if(!@campaign.errors.empty?) }
    %small You can add campaign objectives later.
  #objectives{ hidden_if(@campaign.errors.empty?) }
    %small
      Please specify target number of leads, expected conversion ratio, target revenue, and 
      campaign budget. These numbers will let you track actual campaign performance.
    %table{ :width => 500, :cellpadding => 0, :cellspacing => 0 }
      %tr
        %td{ :valign => :top }
          .label Number of leads:
          = f.text_field :target_leads, :style => "width:110px; text-align:right;"
        %td= spacer
        %td{ :valign => :top }
          .label Conversion (%):
          = f.text_field :target_conversion, :style => "width:110px; text-align:right;"
        %td= spacer
        %td{ :valign => :top }
          .label Revenue ($):
          = f.text_field :target_revenue, :style => "width:110px; text-align:right;"
        %td= spacer
        %td{ :valign => :top }
          .label Budget ($):
          = f.text_field :budget, :style => "width:110px; text-align:right;"
      %tr
        %td{ :colspan => 7, :valign => :top }
          .label Objectives:
          = f.text_area :objectives, :style => "width:500px", :rows => 2
