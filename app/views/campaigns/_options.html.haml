%p
  - per_page = @current_user.preference[:campaigns_per_page] || Campaign.per_page
  - outline = @current_user.preference[:campaigns_outline] || "long"

  = link_to_close options_campaigns_path
  = hidden_field_tag "campaign[user_id]", @current_user.id
  == Sort campaigns by #{link_to("date created", "#", :id => :sort_by)}.
  == Show #{link_to(per_page, "#", :id => :per_page)} campaign records per page
  == using #{link_to(outline, "#", :id => :outline)} format.
%p

%script
  = render :partial => "sort_by"
  = render :partial => "common/per_page"
  = render :partial => "common/outline"
