unless @campaigns.blank?
  page[:campaigns].replace_html render(:partial => "campaign", :collection => @campaigns)
else
  page[:campaigns].replace_html render(:partial => "empty")
end
page[:paginate].replace_html render(:partial => "common/ajax_paginate")
