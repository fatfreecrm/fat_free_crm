- edit ||= false
- collapsed = session[:campaign_comment].nil? # && @campaign.errors.empty?
= subtitle :campaign_comment, collapsed, t(:comment)
.section
  %small#campaign_comment_intro{ hidden_if(!collapsed) }
    = t(:comment_intro)
  #campaign_comment{ hidden_if(collapsed) }
    = text_area_tag :comment