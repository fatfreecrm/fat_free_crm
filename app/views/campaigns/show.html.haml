#confirm{ hidden }
.title_menu
  = link_to("Back to List of Campaigns", campaigns_path) + " | "
  = link_to("Edit", edit_campaign_path(@campaign)) + " | "
  = link_to_function("Delete?", confirm_delete(@campaign))
.title= @campaign.name

