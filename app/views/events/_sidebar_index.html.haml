.filters#filters
  = render "shared/search"
  = hook(:index_event_sidebar_bottom, self)
  - Setting.event_category.each do |key|
    .check_box{:style => "border-bottom: 1px silver dotted;"}
      %div{:style => "float:right;"}
        = @event_category_total[key]
      = event_category_checbox(key, @event_category_total[key])
      = label_tag(key, t(key))

  .check_box{:style => "border-bottom: 1px silver dotted;"}
    %div{:style => "float:right;"}
      = @event_category_total[:other]
    = event_category_checbox("other", @event_category_total[:other])
    = label_tag(:other, t(:other))

  .check_box
    %div{:style => "float:right;"}
      %b= @event_category_total[:all]
    %b #{t :total_events}
  = hook(:index_event_sidebar_bottom, self)
