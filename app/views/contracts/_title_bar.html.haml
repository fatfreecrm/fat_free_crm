#confirm{ hidden }
.title_tools#menu
  = link_to_inline(:edit_contract, edit_contract_path(@contract), text: t(:edit)) + " | "
  = link_to_function(t(:delete) + '?', confirm_delete(@contract))
.title_tools#buttons
  = view_buttons
.title#edit_contract_title
  = h(@contract.name)
  = image_tag("loading.gif", size: :thumb, id: "loading", style: "display: none;")

%div#edit_contract{ hidden }
