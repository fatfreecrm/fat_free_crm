%p
  = link_to_close options_admin_customfields_path
  = hidden_field_tag "customfield[user_id]", @current_user.id
  == Sort Custom fields by #{link_to(@sort_by, "#", :id => :sort_by)}.
  == Show #{link_to(@per_page, "#", :id => :per_page)} customfield records per page
  == using #{link_to(@outline, "#", :id => :outline)} format.
%p

%script
  = render :partial => "sort_by"
  = render :partial => "common/per_page"
  = render :partial => "common/outline"
