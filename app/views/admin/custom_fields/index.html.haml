= styles_for :custom_fields

.title_tools
  = link_to_inline(:create_custom_field, new_admin_custom_field_path)

.title
  %span#create_custom_field_title Custom Fields for #{t(@klass_name.downcase)}

.remote#options{ hidden }
.remote#create_custom_field{ hidden }

.list#custom_fields
  - unless @custom_fields.empty?
    = render :partial => "custom_field", :collection => @custom_fields
  - else
    - # Special case for 'empty' partial for admin controller.
    - assets = controller.controller_name
    - asset = assets.singularize
    - path_prefix = "admin_"
    #empty
      - if @current_query.blank?
        == #{t(:could_not_find, assets)} #{link_to(t(:create_new) << ' ' << t(asset + '_small'), :url => send("new_" << path_prefix << asset << "_path"), :remote => true)}.
      - else
        == #{t(:could_not_find_matching, t(assets.downcase + '_small'))} <span class="cool"><b>#{h @current_query}</b></span>; #{t :please_retry}

-# #paginate= render :partial => "shared/paginate"

