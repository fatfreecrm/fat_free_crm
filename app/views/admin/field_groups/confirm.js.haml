- id = dom_id(@field_group, :confirm)

if (jQuery('##{id}').size() > 0) {
crm.flick('#{id}', 'remove');
} else {
jQuery('##{dom_id(@field_group)}').prepend('#{ j (render :partial => "confirm") }');
}
