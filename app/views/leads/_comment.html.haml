- edit ||= false
- collapsed = session[:lead_comment].nil? # && @lead.errors.empty?
= subtitle :lead_comment, collapsed, t(:comment)
.section
  %small#lead_comment_intro{ hidden_if(!collapsed) }
    = t(:comment_intro)
  #lead_comment{ hidden_if(collapsed) }
    = text_area_tag :comment