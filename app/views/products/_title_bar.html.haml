#confirm{ hidden }
.title_tools#menu
  = link_to_inline(:edit_product, edit_product_path(@product), text: t(:edit)) + " | "
  = link_to_function(t(:delete) + '?', confirm_delete(@product))
.title_tools#buttons
  = view_buttons
.title#edit_product_title
  = h(@product.name)
  = image_tag("loading.gif", size: :thumb, id: "loading", style: "display: none;")

%div#edit_product{ hidden }
