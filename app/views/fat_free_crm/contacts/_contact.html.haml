- if (controller.controller_name == 'contacts') and (template = template_for_current_view).present?
  = render("fat_free_crm/" + template, contact: contact)
- else
  -# default view
  = render 'fat_free_crm/contacts/index_long', contact: contact
