- if (controller.controller_name == 'accounts') and (template = template_for_current_view).present?
  = render('fat_free_crm/' + template, account: account)
- else
  -# default view
  = render 'fat_free_crm/accounts/index_brief', account: account
