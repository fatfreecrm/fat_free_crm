.idc-panel
  .form-group.full-width
    %label #{t :name}:
    = f.text_field( :name, { class: 'form-control' })
  .form-group.full-width
    %label #{t :users}:
    = f.select :user_ids, FatFreeCrm::User.all.map {|u| [u.full_name, u.id]}, {}, multiple: true, class: 'custom-select'
