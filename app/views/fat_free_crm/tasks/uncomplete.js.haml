- if @task.errors.empty?
  $('#fat_free_crm_#{dom_id(@task)}').slideUp(250);

  - if @empty_bucket
    $('#list_#{@empty_bucket}').fadeOut({ duration:500 });

  - if called_from_index_page?
    = refresh_sidebar(:index, :filters)
- else # Errors
  $('#fat_free_crm_#{dom_id(@task)}').effect("shake", { duration:250, distance: 6 });
  $('#task_name').focus();
  $('#task_submit').enable();
