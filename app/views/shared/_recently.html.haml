- @recent = Version.for(@current_user).recent
.panel#recently
  .caption #{t :recent_items}
  - if @recent.present?
    = render :partial => "shared/recent", :collection => @recent
  - else
    %div #{t :no_recent_items}
