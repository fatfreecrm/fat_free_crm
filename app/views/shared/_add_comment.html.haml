- edit ||= false
- collapsed = session[:comment].nil?
= subtitle :comment, collapsed, t(:comment)
.section
  %small#comment_intro{ hidden_if(!collapsed) }
    = t(:comment_intro)
  #campaign_comment{ hidden_if(collapsed) }
    = trix_editor_tag :comment_body, @comment_body, id: :comment_body
