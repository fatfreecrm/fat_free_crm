= inline :create_task, url_for(:action => :new, :view => @view), :text => t(:create_task)
.title#title= (@view == "assigned" ? t(:assigned_tasks) : t(:tasks))
.remote#options{ hidden }
.remote#create_task{ hidden }
