# == Schema Information
# Schema version: 26
#
# Table name: address
#
#  id                 :integer(4)      not null, primary key
#  street1            :string(255)
#  street2            :string(255)
#  city               :string(255)
#  state              :string(255)
#  zipcode            :string(255)
#  country            :string(2)
#  full_address       :string(255)
#  address_type       :string(255)
#  addressable_id     :integer(4)
#  addressable_type   :string(255)
#  created_at         :datetime
#  updated_at         :datetime
#  deleted_at         :datetime

<% 
  require "faker"
  puts "Loading addresses..."

  addressable = %w(Account Contact Lead)
  for i in (1 .. rand(200) + 200) do
%>
address_<%= i %>:
  id               : <%= i %>
  user_id          : <%= rand(8) + 1 %>
  addressable_id   : <%= rand(15) + 1 %>
  addressable_type : <%= addressable[rand(commentable.size)] %>
  street1          : <%= Faker::Address.street_address %>
  street2          : <%= Faker::Address.street_address %>   
  city             : <%= Faker::Address.city }
  state            : <%= Faker::Address.us_state_abbr }
  a.zipcode        : <%= Faker::Address.zip_code }
  a.country        : <%= Faker::Address.country }
  a.full_address   : <%= Factory.next(:address) }

<% end %>